import React from 'react'
import {QuotationWrapper} from './QuotationElements'

const Quotation = ({detailsVisble}) => {
    return (
        <QuotationWrapper detailsVisble={detailsVisble} >
            Quotation
        </QuotationWrapper>
    )
}

export default Quotation
